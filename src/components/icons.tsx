import { ArrowRight, LogoGithub } from "@carbon/icons-react";
import { ElementType, ReactElement } from "react";

export const icons: { [key: string]: ElementType<any, keyof object> } = {
  "logo-github": LogoGithub,
  "arrow-right": ArrowRight
};
