diff --git a/QuantumRaspberryTie.v7_1.py b/QuantumRaspberryTie.v7_1.py
index original..rasqberry 100644
--- a/QuantumRaspberryTie.v7_1.py
+++ b/QuantumRaspberryTie.v7_1.py
@@ -1,3 +1,6 @@
+import sys
+sys.path.insert(0, "/usr/bin")
+
 # -*- coding: utf-8 -*-
 """
 Created on Wed Dec 15 12:56:59 2021
@@ -72,8 +75,9 @@ if qv_backend:
             # SPI(tx=board.D10, clock=board.D11), NUM_PIXELS, pixel_order=PIXEL_ORDER, brightness=BRIGHTNESS, auto_write=False
         # )
         try:
-            import board
-            import neopixel_spi as neopixel
+            # import board
+            from rq_led_utils import get_led_config, create_neopixel_strip
+            import neopixel

             print("Version of python neopixel module: ", neopixel.__version__)
             print()
@@ -1091,14 +1095,12 @@ if __name__ == "__main__":
                     NUM_PIXELS = 64

                 print(f"NUM_PIXELS={NUM_PIXELS}")
-                # Neopixel initialization
-                spi = board.SPI()
-
-                neopixel_array = neopixel.NeoPixel_SPI(
-                    spi,
-                    NUM_PIXELS,
-                    pixel_order=PIXEL_ORDER,
-                    brightness=BRIGHTNESS,
-                    auto_write=False,
+                # Neopixel initialization (PWM/PIO driver auto-detects Pi 4 vs Pi 5)
+                config = get_led_config()
+                neopixel_array = create_neopixel_strip(
+                    config["led_count"],
+                    config["pixel_order"],
+                    brightness=BRIGHTNESS
                 )
                 # neopixel_array.clear()
                 print("Neopixel array intialized")
@@ -469,6 +471,9 @@ if qv_backend:

     def display_to_LEDs(pixel_list, LED_array_indices):
+        # Clear all LEDs before updating the 8x8 grid
+        neopixel_array.fill((0, 0, 0))
+
         for index, pixel in enumerate(pixel_list):
             # Get RGB data from pixel list
             red, green, blue = pixel[0], pixel[1], pixel[2]