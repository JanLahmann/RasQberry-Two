diff --git a/QuantumRaspberryTie.v7_1.py b/QuantumRaspberryTie.v7_1.py
index original..rasqberry 100644
--- a/QuantumRaspberryTie.v7_1.py
+++ b/QuantumRaspberryTie.v7_1.py
@@ -55,6 +55,9 @@
 import sys                             # used to check for passed filename
 import os                              # used to find script directory
+
+# Add /usr/bin to Python path for RasQberry utilities
+sys.path.insert(0, '/usr/bin')
 import platform                        # used to detect architecture
 import psutil                          # used to detect sensehat GUI
 import requests                        # used for ping
@@ -1077,6 +1080,10 @@ if DoSenseHat and not NoHat:

 if UseNeo:
     print("importing neopixel library...")
+
+    # RasQberry utilities for chunked LED writes (supports 192+ LEDs)
+    from rq_led_utils import chunked_show, get_led_config
+
     try:
         import neopixel_spi as neopixel
     except Exception as e:
@@ -477,7 +484,13 @@ def showarraybit(index, bit, neopixel_array):
         neopixel_array[LED_index] = (red, green, blue)

     # Display image after all pixels have been set
-
-    neopixel_array.show()
+
+    # Use chunked writes to support 192+ LEDs
+    # (neopixel_spi buffer limit is 168 LEDs without chunking)
+    # Read chunk parameters from RasQberry environment
+    config = get_led_config()
+    chunk_size = config.get('led_chunk_size', 8)
+    chunk_delay = config.get('led_chunk_delay_ms', 8)
+    chunked_show(neopixel_array, chunk_size=chunk_size, delay_ms=chunk_delay)


 #----------------------------------------------------------------