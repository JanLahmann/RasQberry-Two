diff --git a/display_to_LEDs_from_file.py b/display_to_LEDs_from_file.py
index original..rasqberry 100644
--- a/display_to_LEDs_from_file.py
+++ b/display_to_LEDs_from_file.py
@@ -1,5 +1,10 @@
 # Standard
 import json
+import sys
+import os
+
+# Add /usr/bin to Python path for RasQberry utilities
+sys.path.insert(0, '/usr/bin')
 import argparse
 import atexit

@@ -9,6 +14,9 @@ import neopixel_spi as neopixel

 # Local
 from turn_off_LEDs import turn_off_LEDs
+
+# RasQberry utilities for chunked LED writes (supports 192+ LEDs)
+from rq_led_utils import chunked_show, get_led_config
 from LED_array_indices import LED_array_indices

 # # Neopixel constants
@@ -45,7 +53,13 @@ def display_to_LEDs(array_data, args):
         # Set the appropriate pixel to the RGB value
         pixels[LED_array_index] = (red, green, blue)

-    pixels.show()
+    # Use chunked writes to support 192+ LEDs
+    # (neopixel_spi buffer limit is 168 LEDs without chunking)
+    # Read chunk parameters from RasQberry environment
+    config = get_led_config()
+    chunk_size = config.get('led_chunk_size', 8)
+    chunk_delay = config.get('led_chunk_delay_ms', 8)
+    chunked_show(pixels, chunk_size=chunk_size, delay_ms=chunk_delay)

     if console:
         print("LED Array Data:")