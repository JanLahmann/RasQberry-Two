# ============================================================================
# RasQberry Boot Configuration
# ============================================================================
# This file allows you to configure LED hardware settings BEFORE first boot.
#
# HOW TO USE:
# 1. Remove SD card from Raspberry Pi
# 2. Insert SD card into your computer (Mac/Windows/Linux)
# 3. Open the boot partition (labeled "bootfs")
# 4. Edit this file: rasqberry_boot.env
# 5. Save changes
# 6. Safely eject SD card
# 7. Insert into Raspberry Pi and boot
#
# Your settings will be applied automatically at boot time!
#
# ============================================================================

# ----------------------------------------------------------------------------
# LED Strip Configuration
# ----------------------------------------------------------------------------

# Number of LEDs in your strip/matrix
# Common values: 192 (default 8×24), 256 (16×16), 96 (4×24), 48 (4×12)
# Uncomment to override:
#LED_COUNT=192

# GPIO pin connected to the LED data line
# Default: 18 (supports both Pi 4 PWM and Pi 5 PIO)
# Other common pins: 12, 21
# Uncomment to override:
#LED_GPIO_PIN=18

# LED pixel color order
# Options: RGB, GRB (most common), RGBW, GRBW
# Default: GRB (for WS2812B LEDs)
# If colors appear wrong, try switching between RGB and GRB
# Uncomment to override:
#LED_PIXEL_ORDER=GRB

# Default brightness (0.0 = off, 1.0 = maximum)
# Recommended: 0.2-0.5 for indoor use, 0.6-1.0 for bright environments
# Lower values reduce power consumption and heat
# Uncomment to override:
#LED_DEFAULT_BRIGHTNESS=0.4

# ----------------------------------------------------------------------------
# LED Matrix Layout Configuration
# ----------------------------------------------------------------------------

# Matrix layout type
# Options:
#   single - One continuous serpentine matrix (most common)
#   quad   - Four separate panels wired together
# Uncomment to override:
#LED_MATRIX_LAYOUT=single

# Matrix dimensions (for single layout)
# Standard configurations:
#   8×24  (192 LEDs) - Default RasQberry setup
#   16×16 (256 LEDs) - Square matrix
#   8×32  (256 LEDs) - Wide display
# Uncomment to override:
#LED_MATRIX_WIDTH=24
#LED_MATRIX_HEIGHT=8

# Y-axis flip (if matrix appears upside down)
# Options: true, false
# Uncomment to override:
#LED_MATRIX_Y_FLIP=false

# ----------------------------------------------------------------------------
# Quad Panel Configuration (only used if LED_MATRIX_LAYOUT=quad)
# ----------------------------------------------------------------------------

# Individual panel dimensions
# Default: 12×4 (48 LEDs per panel, 192 total for 4 panels)
# LED_MATRIX_PANEL_WIDTH=12
# LED_MATRIX_PANEL_HEIGHT=4

# ----------------------------------------------------------------------------
# Advanced LED Settings (rarely need changing)
# ----------------------------------------------------------------------------

# LED signal frequency in Hz
# Default: 800000 (800 kHz for WS2812B)
# Some older LEDs use 400000 (400 kHz)
# LED_FREQ_HZ=800000

# DMA channel for PWM (Pi 4 only)
# Default: 10
# LED_DMA=10

# LED PWM channel
# Options: 0 (default), 1
# Use 1 for GPIOs 13, 19, 41, 45, or 53
# LED_CHANNEL=0

# Signal inversion (when using NPN transistor level shifter)
# Options: true, false
# LED_INVERT=false

# ----------------------------------------------------------------------------
# Example Configurations
# ----------------------------------------------------------------------------

# Example 1: Standard 8×24 single serpentine matrix (default)
# LED_COUNT=192
# LED_MATRIX_LAYOUT=single
# LED_MATRIX_WIDTH=24
# LED_MATRIX_HEIGHT=8

# Example 2: Large 16×16 square matrix
# LED_COUNT=256
# LED_MATRIX_LAYOUT=single
# LED_MATRIX_WIDTH=16
# LED_MATRIX_HEIGHT=16

# Example 3: Four 12×4 panels in quad layout
# LED_COUNT=192
# LED_MATRIX_LAYOUT=quad
# LED_MATRIX_PANEL_WIDTH=12
# LED_MATRIX_PANEL_HEIGHT=4

# Example 4: Wide 32×8 display
# LED_COUNT=256
# LED_MATRIX_LAYOUT=single
# LED_MATRIX_WIDTH=32
# LED_MATRIX_HEIGHT=8

# ============================================================================
# End of RasQberry Boot Configuration
# ============================================================================